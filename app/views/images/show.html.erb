<%= image_tag(@image.url) %>
<%= render 'tag_list', image: @image %>
<%= link_to 'delete image', image_path(@image), method: :delete, class: 'js-delete-image-link',
  data: { confirm: 'Are you sure?' } %>