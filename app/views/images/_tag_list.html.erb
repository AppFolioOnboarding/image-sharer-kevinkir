<% unless image.tag_list.empty? %>
  <div>
    <ul class="tag-list">
    <% image.tag_list.each do |tag| %>
      <li>
        <%= link_to tag, images_path(tag: tag), class: 'image-tag' %>
      </li>
    <% end %>
    </ul>
  </div>
<% end %>